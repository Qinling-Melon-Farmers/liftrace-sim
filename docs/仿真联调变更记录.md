
## 2026-09-19：起飞FLU场景转换
- tools/rotate_start_frame.py将世界/靶位/任务航点/门Gate/边界从旧坐标转为X朝场内、Y向左；轴对齐箱体交换尺寸，树与靶同步转yaw。冻结旧run目标，防止旋转后同seed重新抽样改变布局。
- 拒绝非零容器姿态、非轴对齐墙体与非箱体link，防止静默错误；只处理离线场景，不发布控制数据。
- 验证79项单测通过；新场景五靶足迹/间距及启动参数通过。后续SITL在集成研究分支进行，上游main不改。
